<!DOCTYPE html>
<html lang="pl">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf8">
    <title>Show quantity</title>
    <link rel='stylesheet' href='/stylesheets/bootstrap.css'/>
    <link rel="stylesheet" href="/stylesheets/style.css"/>
    <script src="/javascripts/setBodyPadding.js"></script>
    <script src="/javascripts/show-quantity.js"></script>
    <script>
      let data = <%- JSON.stringify(data) %>;
    </script>
</head>
<% include navbar.ejs %>
<body>
<div class="container form">
    <h1>Show quantity</h1>
    <form>
        <fieldset>
            <div class="col-form-label">
                <label for="typeSelect">Product</label>
                <div id="selections" class="form-group product">
                    <select class="form-control" required id="typeSelect">
                        <option disabled selected>Product type</option>
                        <option value="beers">Beers</option>
                        <option value="wines">Wines</option>
                        <option value="liquors">Liquors</option>
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label class="col-form-label" for="dateInput">Date</label>
                <input type="date" min="" class="form-control" id="dateInput" required=>
            </div>

            <div class="form-group" style="margin-top: 40px">
                <div class="card text-white bg-primary mb-3">
                    <div class="card-header">Quantity</div>
                    <div class="card-body">
                        <h2 id="quantity">
                        </h2>
                    </div>
                </div>
            </div>

            <div class="buttons">
                <button onclick="location.href='/'" type="button" class="btn btn-primary btn-lg ">Back</button>
            </div>
        </fieldset>
    </form>
</div>
<% include footer.ejs %>
</body>
</html>
