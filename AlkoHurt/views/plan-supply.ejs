<!DOCTYPE html>
<html lang="pl">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf8">
    <title>Plan supply</title>
    <link rel='stylesheet' href='/stylesheets/bootstrap.css' />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="/javascripts/plan-supply.js"></script>
    <script>
        let data = <%- JSON.stringify(data)%>;
    </script>
</head>
<%include navbar%>
<body>
<div class="container form">
    <h1>Plan supply</h1>
    <form onsubmit="sendRequest()">
        <fieldset>
            <div class="form-group">
                <label class="col-form-label" for="supplierSelect">Supplier</label>
                <select onchange=updateSupplierAndDate() class="form-control" id="supplierSelect" required name="supplier">

                </select>
            </div>
            <div class="form-group">
                <label class="col-form-label" for="dateInput">Date</label>
                <input onchange="updateSupplierAndDate()" type="date" min="" class="form-control" id="dateInput" required="required">
            </div>
            <div id="selections" class="col-form-label">
            </div>
            <div class="buttons">
                <button onclick="location.href='/'" type="button" class="btn btn-primary btn-lg ">Back</button>
                <button onclick="addNewProductField()" type="button" class="btn btn-primary btn-lg">Add product</button>
                <button type="submit" class="btn btn-primary btn-lg">Submit</button>

            </div>
        </fieldset>
    </form>
    <footer>
        <%include footer.ejs%>
    </footer>
</div>
</body>